query GetViewer {
  viewer {
    login
    name
    email
    company
    createdAt
    repositories(first: 10, orderBy: { field: UPDATED_AT, direction: DESC }) {
      totalCount
      nodes {
        name
        description
        url
        stargazerCount
        forkCount
        updatedAt
      }
    }
  }
}
